# ==========================================
# 60_HIDEANDSEEK — ENV TEMPLATE
# Source of Truth: KeePassXC Database
# - DB: /home/akki/vault/secrets/Secrets.kdbx
# - Keyfile: /home/akki/vault/secrets/Secrets.keyx
# - Entries: 02_Global/.env (Username: Global), 03_Projects/.env (Username: 60_HIDEANDSEEK)
# This file defines the whitelist of allowed keys (NO real values).
# Generate `.env` via: pnpm env:pull
# ==========================================

# --- REQUIRED KEYS (missing -> exit 1) ---
# --- GLOBAL (KeePassXC: 02_Global/.env) ---
#GITHUB_TOKEN=
#OPENAI_API_KEY=
#NGROK_AUTHTOKEN=

# --- PROJECT (KeePassXC: 03_Projects/.env) ---
FRONTEND_PORT=
BACKEND_HOST=
BACKEND_PORT=
APP_BASE_URL=
VITE_APP_BASE_URL=
SQLITE_DB_PATH=
DATABASE_URL=
VITE_ALLOWED_HOSTS=

# OPTIONAL
# --- OPTIONAL KEYS (missing -> warning only) ---
VITE_SOURCEMAP=
