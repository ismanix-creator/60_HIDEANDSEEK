# @file        .env.example
# @description Environment Template (for reference only)
# @version     0.2.0
# @created     2026-01-06 19:14:38 CET
# @updated     2026-01-08 02:45:00 CET
# @author      agenten-koordinator
# @changelog
#   0.2.0 - 2026-01-08 - Updated to reflect global ENV usage with HIDEANDSEEK_ prefix
#   0.1.0 - 2026-01-06 - Initial scaffold

# IMPORTANT: This project uses /home/akki/.config/.env as Source of Truth.
# This file is for documentation purposes only.
# Do NOT create a local .env file in this project.
#
# Add these variables to /home/akki/.config/.env with HIDEANDSEEK_ prefix:

# Frontend Dev Server Port
HIDEANDSEEK_FRONTEND_PORT=5173

# Backend API Host
HIDEANDSEEK_BACKEND_HOST=localhost

# Backend API Port
HIDEANDSEEK_BACKEND_PORT=3001

# Vite Allowed Hosts (CSV)
HIDEANDSEEK_VITE_ALLOWED_HOSTS=localhost,.ngrok-free.dev

# App Base URL (Frontend)
HIDEANDSEEK_APP_BASE_URL=http://localhost:5173

# Vite App Base URL (for client-side)
HIDEANDSEEK_VITE_APP_BASE_URL=http://localhost:5173

# SQLite Database Path (relative to project root)
HIDEANDSEEK_SQLITE_DB_PATH=data/material-tracker.db

# Optional: Enable sourcemaps in production
# VITE_SOURCEMAP=true

# Optional: Node environment
# NODE_ENV=development
